name: Manual Workflow
on:
  repository_dispatch:
    types: [ak]
    inputs: 
    schedule:          # 定时任务 第一个填分钟 第二个填小时
      - cron:  59 9 * * *   # 想要运行的小时-8即为填入值例如：bj11点运行 填11-8=3
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: echo Hello ${{ github.event.inputs.name }}!
